# 招标文件合规性审查引擎 — 实施计划

> **For agentic workers:** Use superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 新增"招标文件合规审查"分析模式：上传招标文件 → 条款结构化提取 → 规则引擎判定(红/黄/绿) → 合规评分 + 报告

**Architecture:** 复用现有 Parser + Orchestrator 模式，新增 3 张数据表(compliance_analyses/clauses/rules)、条款提取层(粗拆规则+精拆LLM)、规则引擎(8确定性+2LLM语义)、扣分模型。前端在项目详情页新增"合规审查"Tab。

**Tech Stack:** Python 3.10+ / FastAPI / SQLAlchemy 2.0 / Gitee AI API / React 18 + Ant Design 5 / TypeScript

## Global Constraints

- 不修改现有投标分析逻辑（`analysis_orchestrator` 的 8 维度保持不变）
- 复用 `BidDocument` 存储招标文件、复用 `document_parser` 解析、复用 `gitee_client.py` 调用 LLM
- 规则参数配置化，存储在 `data/compliance_config.json` + `compliance_rules` 表
- TDD：先写测试，后实现
- 每个 task 以 `git commit` 结束

---

## File Map

```
backend/
├── app/
│   ├── models/compliance.py              [NEW] 3 ORM models
│   ├── schemas/compliance.py             [NEW] Pydantic schemas
│   ├── services/analysis/
│   │   ├── compliance_clause_extractor.py [NEW] coarse + fine extraction
│   │   ├── compliance_rule_engine.py      [NEW] deterministic + LLM rules
│   │   ├── compliance_scorer.py           [NEW] deduction scoring
│   │   └── compliance_orchestrator.py     [NEW] full pipeline orchestrator
│   └── api/v1/compliance.py              [NEW] REST endpoints
├── data/compliance_config.json           [NEW] default rules + clause types
└── tests/unit/
    ├── test_compliance_clause_extractor.py [NEW]
    ├── test_compliance_rule_engine.py      [NEW]
    ├── test_compliance_scorer.py           [NEW]
    └── test_compliance_orchestrator.py     [NEW]

frontend/src/
├── types/index.ts                        [MODIFY] add Compliance* types
├── services/api.ts                       [MODIFY] add complianceApi
└── pages/ProjectDetail/
    ├── index.tsx                         [MODIFY] add compliance Tab
    └── components/CompliancePanel.tsx    [NEW] compliance results display
```

---

### Task 1: 数据模型 + 配置文件

**Files:**
- Create: `backend/app/models/compliance.py`
- Create: `backend/data/compliance_config.json`
- Create: `backend/tests/unit/test_compliance_models.py`

**Interfaces:**
- Produces: `ComplianceAnalysis`, `ComplianceClause`, `ComplianceRule` ORM models
- Produces: `load_compliance_config() -> dict` 配置加载函数

- [ ] **Step 1: 创建 compliance_config.json**

```json
{
  "clause_types": [
    {"id": "qualification", "label": "资格条件", "active": true},
    {"id": "performance", "label": "业绩要求", "active": true},
    {"id": "technical", "label": "技术要求", "active": true},
    {"id": "timeline", "label": "时间节点", "active": true},
    {"id": "scoring", "label": "评分标准", "active": true},
    {"id": "contract", "label": "合同条款", "active": true},
    {"id": "other", "label": "其他", "active": true}
  ],
  "scoring": {
    "initial_score": 100,
    "red_deduction": 20,
    "yellow_deduction": 8,
    "risk_low": 85,
    "risk_medium": 60,
    "risk_high": 40
  },
  "default_rules": [
    {
      "id": "R01", "name": "发售期不足", "category": "deterministic",
      "is_active": true, "default_risk": "red", "weight": 0.20,
      "conditions": {"clause_type": "timeline", "field": "天数", "field_label": "发售期"},
      "params": {"min_days": 5},
      "legal_basis": [{"law": "招投标法实施条例", "article": "第16条", "content": "资格预审文件或者招标文件的发售期不得少于5日"}]
    },
    {
      "id": "R02", "name": "资质要求过高", "category": "deterministic",
      "is_active": true, "default_risk": "yellow", "weight": 0.15,
      "conditions": {"clause_type": "qualification"},
      "params": {"max_grade": 2},
      "legal_basis": [{"law": "招投标法", "article": "第18条", "content": "招标人不得以不合理的条件限制或者排斥潜在投标人"}]
    },
    {
      "id": "R03", "name": "业绩门槛过高", "category": "deterministic",
      "is_active": true, "default_risk": "yellow", "weight": 0.15,
      "conditions": {"clause_type": "performance"},
      "params": {"amount_multiplier": 2.0, "max_years": 3},
      "legal_basis": [{"law": "招投标法实施条例", "article": "第32条", "content": "招标人不得以不合理的条件限制、排斥潜在投标人或者投标人"}]
    },
    {
      "id": "R04", "name": "品牌指向性", "category": "deterministic",
      "is_active": true, "default_risk": "red", "weight": 0.20,
      "conditions": {"clause_type": "technical"},
      "params": {"min_brands": 3, "equivalent_keywords": ["或同等", "或相当于", "或类似"]},
      "legal_basis": [{"law": "招投标法实施条例", "article": "第32条", "content": "限定或者指定特定的专利、商标、品牌、原产地或者供应商"}]
    },
    {
      "id": "R05", "name": "主观分过高", "category": "deterministic",
      "is_active": true, "default_risk": "yellow", "weight": 0.10,
      "conditions": {"clause_type": "scoring"},
      "params": {"max_subjective_ratio": 0.40},
      "legal_basis": [{"law": "招投标法", "article": "第18条", "content": "招标人不得以不合理的条件限制或者排斥潜在投标人"}]
    },
    {
      "id": "R07", "name": "保证金过高", "category": "deterministic",
      "is_active": true, "default_risk": "yellow", "weight": 0.10,
      "conditions": {"clause_type": "contract"},
      "params": {"max_ratio": 0.02},
      "legal_basis": [{"law": "招投标法实施条例", "article": "第26条", "content": "投标保证金不得超过招标项目估算价的2%"}]
    },
    {
      "id": "R08", "name": "地域限制", "category": "deterministic",
      "is_active": true, "default_risk": "red", "weight": 0.15,
      "conditions": {"clause_type": ["qualification", "performance", "scoring"]},
      "params": {"region_keywords": ["本地", "本省", "本市", "本地区", "当地", "属地"]},
      "legal_basis": [{"law": "招投标法实施条例", "article": "第32条", "content": "限定或者指定特定的专利、商标、品牌、原产地或者供应商"}]
    },
    {
      "id": "R10", "name": "隐性排他条款", "category": "llm_semantic",
      "is_active": true, "default_risk": "yellow", "weight": 0.15,
      "conditions": {"clause_type": ["qualification", "performance", "technical"]},
      "params": {},
      "legal_basis": [{"law": "招投标法", "article": "第18条", "content": "招标人不得以不合理的条件限制或者排斥潜在投标人"}],
      "llm_prompt": "你是一名招投标合规审查专家。请判断以下招标文件条款是否构成对特定投标人的隐性排他或歧视。条款原文：\n---\n{clause_text}\n---\n条款类型：{clause_type}\n结构化参数：{params}\n\n请从以下维度分析：\n1. 该条款是否超出项目实际需要\n2. 该条款是否客观上只有极少数企业能满足\n3. 该条款是否含有不合理的限制\n\n输出 JSON：{\"risk\": \"red|yellow|green\", \"reason\": \"判断理由(50字以内)\", \"confidence\": 0.0-1.0}"
    },
    {
      "id": "R11", "name": "萝卜坑综合研判", "category": "llm_semantic",
      "is_active": true, "default_risk": "red", "weight": 0.20,
      "conditions": {"clause_type": ["qualification", "performance", "technical", "scoring"]},
      "params": {"min_clauses": 3},
      "legal_basis": [{"law": "招投标法实施条例", "article": "第32条", "content": "招标人不得以不合理的条件限制、排斥潜在投标人或者投标人"}],
      "llm_prompt": "你是一名招投标合规审查专家。请综合研判以下招标文件的多个条款组合，是否构成'量体裁衣'——即多个条款的组合形成唯一指向，只有特定投标人能够同时满足。\n\n以下条款已被确定性规则标记为异常：\n---\n{clauses_summary}\n---\n\n请判断：这些条款组合是否构成'萝卜坑'？\n输出 JSON：{\"risk\": \"red|yellow|green\", \"reason\": \"综合研判理由(100字以内)\", \"confidence\": 0.0-1.0, \"suspect_bidder_profile\": \"可能受益的投标人画像(如可推断)\"}"
    }
  ]
}
```

- [ ] **Step 2: 创建 ORM 模型 `backend/app/models/compliance.py`**

```python
"""招标文件合规审查数据模型"""
from __future__ import annotations

import uuid
from datetime import datetime
from decimal import Decimal
from typing import Optional

from sqlalchemy import (
    JSON, Boolean, DateTime, ForeignKey, Integer, Numeric, String, Text,
)
from sqlalchemy.orm import Mapped, mapped_column, relationship

from app.models.base import Base, TimestampMixin


class ComplianceRule(Base, TimestampMixin):
    """合规审查规则表"""
    __tablename__ = "compliance_rules"

    id: Mapped[str] = mapped_column(
        String(50), primary_key=True, comment="规则ID (R01,R02,...)"
    )
    name: Mapped[str] = mapped_column(String(100), nullable=False, comment="规则名称")
    category: Mapped[str] = mapped_column(
        String(20), nullable=False, comment="deterministic|llm_semantic"
    )
    is_active: Mapped[bool] = mapped_column(Boolean, default=True, comment="启用状态")
    default_risk: Mapped[str] = mapped_column(
        String(20), nullable=False, comment="默认风险等级: red/yellow/green"
    )
    weight: Mapped[Optional[Decimal]] = mapped_column(
        Numeric(3, 2), nullable=True, comment="评分权重"
    )
    conditions: Mapped[Optional[dict]] = mapped_column(JSON, nullable=True, comment="适用条件")
    params: Mapped[Optional[dict]] = mapped_column(JSON, nullable=True, comment="可配置参数")
    legal_basis: Mapped[Optional[dict]] = mapped_column(JSON, nullable=True, comment="引用法条")
    llm_prompt: Mapped[Optional[str]] = mapped_column(Text, nullable=True, comment="LLM prompt模板")

    def __repr__(self) -> str:
        return f"<ComplianceRule(id={self.id}, name='{self.name}')>"


class ComplianceAnalysis(Base, TimestampMixin):
    """合规审查分析任务表"""
    __tablename__ = "compliance_analyses"

    id: Mapped[str] = mapped_column(
        String(36), primary_key=True, default=lambda: str(uuid.uuid4())
    )
    project_id: Mapped[str] = mapped_column(
        String(36), ForeignKey("projects.id", ondelete="CASCADE"), nullable=False
    )
    document_id: Mapped[str] = mapped_column(
        String(36), ForeignKey("bid_documents.id", ondelete="CASCADE"), nullable=False
    )
    status: Mapped[str] = mapped_column(
        String(50), default="pending", comment="pending/analyzing/completed/failed"
    )
    progress: Mapped[int] = mapped_column(Integer, default=0, comment="进度 0-100")
    compliance_score: Mapped[Optional[Decimal]] = mapped_column(
        Numeric(5, 2), nullable=True, comment="合规评分 0-100"
    )
    risk_level: Mapped[Optional[str]] = mapped_column(
        String(20), nullable=True, comment="low/moderate/high/critical"
    )
    clause_count: Mapped[int] = mapped_column(Integer, default=0, comment="提取的条款总数")
    violation_count: Mapped[int] = mapped_column(Integer, default=0, comment="违规条款数")
    error_message: Mapped[Optional[str]] = mapped_column(Text, nullable=True)
    started_at: Mapped[Optional[datetime]] = mapped_column(DateTime(timezone=True), nullable=True)
    completed_at: Mapped[Optional[datetime]] = mapped_column(DateTime(timezone=True), nullable=True)

    clauses: Mapped[list["ComplianceClause"]] = relationship(
        "ComplianceClause", back_populates="analysis", cascade="all, delete-orphan"
    )

    def __repr__(self) -> str:
        return f"<ComplianceAnalysis(id={self.id}, status='{self.status}')>"


class ComplianceClause(Base, TimestampMixin):
    """合规审查条款结果表"""
    __tablename__ = "compliance_clauses"

    id: Mapped[str] = mapped_column(
        String(36), primary_key=True, default=lambda: str(uuid.uuid4())
    )
    analysis_id: Mapped[str] = mapped_column(
        String(36), ForeignKey("compliance_analyses.id", ondelete="CASCADE"), nullable=False
    )
    clause_type: Mapped[str] = mapped_column(String(50), nullable=False, comment="条款类型")
    original_text: Mapped[str] = mapped_column(Text, nullable=False, comment="条款原文")
    location: Mapped[Optional[dict]] = mapped_column(JSON, nullable=True, comment="章节/页码")
    params: Mapped[Optional[dict]] = mapped_column(JSON, nullable=True, comment="结构化参数")
    risk_level: Mapped[Optional[str]] = mapped_column(
        String(20), nullable=True, comment="green/yellow/red"
    )
    matched_rules: Mapped[Optional[dict]] = mapped_column(
        JSON, nullable=True, comment="命中的规则列表及违规依据"
    )

    analysis: Mapped["ComplianceAnalysis"] = relationship(
        "ComplianceAnalysis", back_populates="clauses"
    )

    def __repr__(self) -> str:
        return f"<ComplianceClause(id={self.id}, type='{self.clause_type}', risk='{self.risk_level}')>"
```

- [ ] **Step 3: 创建配置加载函数 `backend/app/core/compliance_config.py`**

```python
"""合规审查配置加载"""
import json
from pathlib import Path
from typing import Any

_CONFIG_PATH = Path(__file__).resolve().parent.parent.parent / "data" / "compliance_config.json"

_config_cache: dict[str, Any] | None = None


def load_compliance_config() -> dict[str, Any]:
    global _config_cache
    if _config_cache is not None:
        return _config_cache
    if _CONFIG_PATH.exists():
        with open(_CONFIG_PATH, encoding="utf-8") as f:
            _config_cache = json.load(f)
    else:
        _config_cache = {"clause_types": [], "scoring": {}, "default_rules": []}
    return _config_cache


def get_active_clause_types() -> list[dict]:
    cfg = load_compliance_config()
    return [t for t in cfg.get("clause_types", []) if t.get("active")]


def get_active_rules() -> list[dict]:
    cfg = load_compliance_config()
    return [r for r in cfg.get("default_rules", []) if r.get("is_active")]
```

- [ ] **Step 4: 创建模型测试 `backend/tests/unit/test_compliance_models.py`**

```python
"""测试 Compliance 数据模型"""
import pytest
from sqlalchemy import select

from app.models.compliance import ComplianceAnalysis, ComplianceClause, ComplianceRule

pytestmark = pytest.mark.asyncio


class TestComplianceRule:
    async def test_create_rule(self, db_session_factory):
        import uuid
        async with db_session_factory() as db:
            rule = ComplianceRule(
                id="R01", name="发售期不足", category="deterministic",
                is_active=True, default_risk="red", weight=0.20,
                conditions={"clause_type": "timeline"},
                params={"min_days": 5},
                legal_basis=[{"law": "实施条例", "article": "第16条"}],
            )
            db.add(rule)
            await db.commit()
            await db.refresh(rule)
            assert rule.id == "R01"
            assert rule.is_active is True
            assert rule.params["min_days"] == 5


class TestComplianceAnalysis:
    async def test_create_analysis(self, db_session_factory, sample_project, sample_documents):
        doc1, _ = sample_documents
        import uuid
        async with db_session_factory() as db:
            analysis = ComplianceAnalysis(
                id=str(uuid.uuid4()),
                project_id=sample_project.id,
                document_id=doc1.id,
                status="pending",
            )
            db.add(analysis)
            await db.commit()
            await db.refresh(analysis)
            assert analysis.status == "pending"

    async def test_analysis_has_clauses(self, db_session_factory, sample_project, sample_documents):
        doc1, _ = sample_documents
        import uuid
        a_id = str(uuid.uuid4())
        async with db_session_factory() as db:
            analysis = ComplianceAnalysis(
                id=a_id, project_id=sample_project.id, document_id=doc1.id,
            )
            db.add(analysis)
            await db.commit()

            clause = ComplianceClause(
                id=str(uuid.uuid4()), analysis_id=a_id,
                clause_type="qualification",
                original_text="投标人须具有壹级资质",
                params={"资质等级": "壹级"},
                risk_level="yellow",
            )
            db.add(clause)
            await db.commit()
            await db.refresh(analysis)
            assert len(analysis.clauses) == 1
```

- [ ] **Step 5: 运行测试并提交**

```bash
pytest tests/unit/test_compliance_models.py -v
```

```bash
git add backend/app/models/compliance.py backend/data/compliance_config.json \
        backend/app/core/compliance_config.py backend/tests/unit/test_compliance_models.py
git commit -m "feat(compliance): add data models + config for bidding doc review"
```

---

### Task 2: 条款结构化提取 — 粗拆

**Files:**
- Create: `backend/app/services/analysis/compliance_clause_extractor.py`
- Modify: `backend/tests/unit/test_compliance_clause_extractor.py` (write tests first)

**Interfaces:**
- Produces: `coarse_split(text: str) -> list[dict]` — 按章节/编号将全文切分为段落块
- Produces: `extract_clauses(text: str, clause_types: list[dict]) -> list[dict]` — 完整提取（粗拆+LLM精拆，LLM部分在Task 3实现）

- [ ] **Step 1: 先写粗拆测试**

```python
"""测试条款提取器"""
import pytest
from app.services.analysis.compliance_clause_extractor import coarse_split

# 运行前模块尚不存在，预期 IMPORT ERROR (Red)

class TestCoarseSplit:
    def test_split_by_chapter_headings(self):
        text = "第一章 招标公告\n1.1 项目概况\n这是招标项目的背景描述。\n\n第二章 投标人须知\n2.1 资格条件\n投标人须具有相关资质。"
        chunks = coarse_split(text)
        assert len(chunks) >= 2
        assert any("第一章" in c["title"] for c in chunks)

    def test_split_by_numbered_sections(self):
        text = "1. 总则\n这是总则内容。\n\n2. 资格要求\n2.1 基本条件\n需要营业执照。\n2.2 业绩要求\n需要3个同类项目。"
        chunks = coarse_split(text)
        assert len(chunks) >= 2

    def test_empty_text(self):
        assert coarse_split("") == []

    def test_single_section(self):
        text = "无章节标题的简单招标文件内容。"
        chunks = coarse_split(text)
        assert len(chunks) == 1

    def test_output_structure(self):
        text = "第一章 招标公告\n这是内容。"
        chunks = coarse_split(text)
        for c in chunks:
            assert "title" in c
            assert "content" in c
            assert "start_line" in c
```

- [ ] **Step 2: 运行测试验证 Red**

```bash
pytest tests/unit/test_compliance_clause_extractor.py -v
# Expected: ImportError (module not yet created)
```

- [ ] **Step 3: 实现粗拆函数**

```python
"""条款结构化提取引擎"""
from __future__ import annotations

import re
from typing import Any

# 中文招标文件常见章节边界模式
_CHAPTER_PATTERNS = [
    re.compile(r'^第[一二三四五六七八九十\d]+章'),       # 第X章
    re.compile(r'^第[一二三四五六七八九十\d]+节'),       # 第X节
    re.compile(r'^[一二三四五六七八九十]+、'),           # 一、
    re.compile(r'^\d+[\.\、]'),                         # 1. / 1、
]


def coarse_split(text: str) -> list[dict[str, Any]]:
    """按章节/编号将招标文件全文切分为段落块。

    Args:
        text: 招标文件全文

    Returns:
        list[dict]: [{"title": "章节标题", "content": "段落内容", "start_line": 0}, ...]
    """
    if not text or not text.strip():
        return []

    lines = text.split('\n')
    if len(lines) <= 3:
        return [{"title": "全文", "content": text, "start_line": 0}]

    # 找出章节边界行
    boundaries: list[int] = []
    boundary_titles: list[str] = []

    for i, line in enumerate(lines):
        stripped = line.strip()
        if not stripped or len(stripped) > 100:
            continue
        for pattern in _CHAPTER_PATTERNS:
            if pattern.match(stripped):
                boundaries.append(i)
                boundary_titles.append(stripped[:80])
                break

    # 无章节边界 → 整个文档作为一个块
    if len(boundaries) <= 1:
        return [{"title": "全文", "content": text, "start_line": 0}]

    # 按边界切分
    chunks: list[dict[str, Any]] = []
    for idx in range(len(boundaries)):
        start = boundaries[idx]
        end = boundaries[idx + 1] if idx + 1 < len(boundaries) else len(lines)
        chunk_text = '\n'.join(lines[start:end]).strip()
        if chunk_text:
            chunks.append({
                "title": boundary_titles[idx],
                "content": chunk_text,
                "start_line": start,
            })

    return chunks
```

- [ ] **Step 4: 运行测试验证 Green**

```bash
pytest tests/unit/test_compliance_clause_extractor.py::TestCoarseSplit -v
# Expected: 5 passed
```

- [ ] **Step 5: Commit**

```bash
git add backend/app/services/analysis/compliance_clause_extractor.py \
        backend/tests/unit/test_compliance_clause_extractor.py
git commit -m "feat(compliance): add coarse clause splitter"
```

---

### Task 3: 条款结构化提取 — LLM 精拆

**Files:**
- Modify: `backend/app/services/analysis/compliance_clause_extractor.py` (add extract_clauses)
- Modify: `backend/tests/unit/test_compliance_clause_extractor.py` (add LLM extraction tests)

**Interfaces:**
- Consumes: `coarse_split(text) -> list[dict]` from Task 2
- Consumes: `GiteeEmbeddingClient` from `backend/app/services/embedding/gitee_client.py` (复用其 httpx 模式，新增 chat completion 方法或新建轻量客户端)
- Produces: `extract_clauses(text: str, clause_types: list[dict]) -> list[dict]`
- Produces: `build_extraction_prompt(chunk_text: str, clause_types: list[dict]) -> str`

- [ ] **Step 1: 写精拆测试**

```python
# 追加到 test_compliance_clause_extractor.py

class TestExtractClauses:
    def test_extract_with_mock_llm(self, monkeypatch):
        """用 mock LLM 响应测试完整提取流程。"""
        from app.services.analysis.compliance_clause_extractor import extract_clauses

        mock_response = {
            "clauses": [
                {
                    "type": "qualification",
                    "original_text": "投标人须具有建筑智能化工程专业承包壹级资质",
                    "location": {"chapter": "第二章", "section": "2.1"},
                    "params": {"资质等级": "壹级", "专业": "建筑智能化"},
                },
                {
                    "type": "performance",
                    "original_text": "近三年内至少完成3个同类项目",
                    "location": {"chapter": "第二章", "section": "2.2"},
                    "params": {"项目数量": 3, "年限": 3},
                },
            ]
        }

        async def mock_chat(*args, **kwargs):
            import json
            return json.dumps(mock_response)

        monkeypatch.setattr(
            "app.services.analysis.compliance_clause_extractor._call_llm_chat",
            mock_chat,
        )

        clause_types = [
            {"id": "qualification", "label": "资格条件"},
            {"id": "performance", "label": "业绩要求"},
        ]
        text = "第二章 投标人须知\n2.1 投标人须具有建筑智能化工程专业承包壹级资质\n2.2 近三年内至少完成3个同类项目"
        clauses = extract_clauses(text, clause_types)
        assert len(clauses) >= 2

    def test_build_extraction_prompt(self):
        from app.services.analysis.compliance_clause_extractor import build_extraction_prompt
        prompt = build_extraction_prompt(
            "投标人须具有壹级资质",
            [{"id": "qualification", "label": "资格条件"}],
        )
        assert "壹级资质" in prompt
        assert "qualification" in prompt
        assert "资格条件" in prompt
```

- [ ] **Step 2: 实现 LLM 调用 + 提取逻辑**

```python
# 追加到 compliance_clause_extractor.py

import json as _json
from app.core.compliance_config import get_active_clause_types


def build_extraction_prompt(chunk_text: str, clause_types: list[dict]) -> str:
    type_list = "\n".join(
        f"- {t['id']}: {t['label']}" for t in clause_types
    )
    return f"""你是一名招投标文件结构化分析专家。请从以下招标文件段落中提取所有独立的资格/要求条款。

支持的条款类型：
{type_list}

对于每个条款，提取：
1. type: 条款类型ID (从上述列表选择最匹配的)
2. original_text: 条款原文（精确摘录）
3. location: 章节/小节信息（如可识别）
4. params: 结构化关键参数（按条款类型动态变化，例如：
   - 资格条件类：资质等级、证书名称、注册资本
   - 业绩要求类：项目数量、合同金额、年限
   - 技术要求类：品牌、型号、技术参数
   - 时间节点类：天数、时间类型（发售期/澄清期/投标期）
   - 评分标准类：分值、客观/主观标识
   - 合同条款类：金额、比例、期限
   ）

段落原文：
---
{chunk_text[:3000]}
---

请以 JSON 格式输出：{{"clauses": [...]}}。如果段落中没有可提取的条款，返回 {{"clauses": []}}。"""


async def _call_llm_chat(prompt: str) -> str:
    """调用 Gitee AI Chat Completion API。"""
    import httpx
    from app.core.config import settings

    url = f"{settings.GITEE_AI_BASE_URL}/chat/completions"
    headers = {"Authorization": f"Bearer {settings.GITEE_AI_API_KEY}"}
    payload = {
        "model": "Qwen3-32B",
        "messages": [{"role": "user", "content": prompt}],
        "temperature": 0.1,
        "max_tokens": 2000,
    }
    async with httpx.AsyncClient(timeout=60) as client:
        resp = await client.post(url, json=payload, headers=headers)
        resp.raise_for_status()
        data = resp.json()
        return data["choices"][0]["message"]["content"]


def _parse_llm_response(raw: str) -> list[dict]:
    """解析 LLM 返回的结构化条款列表。"""
    try:
        # 处理可能的 markdown 代码块包裹
        text = raw.strip()
        if text.startswith("```"):
            lines = text.split("\n")
            text = "\n".join(lines[1:-1])
        data = _json.loads(text)
        return data.get("clauses", [])
    except (_json.JSONDecodeError, KeyError):
        return []


def extract_clauses(
    text: str,
    clause_types: list[dict] | None = None,
) -> list[dict[str, Any]]:
    """从招标文件全文提取条款卡片列表。

    先粗拆为段落块，逐块调用 LLM 做精拆，汇总返回。

    Args:
        text: 招标文件全文
        clause_types: 启用的条款类型列表，默认从配置读取

    Returns:
        list[dict]: 条款卡片列表
    """
    if clause_types is None:
        clause_types = get_active_clause_types()

    if not clause_types:
        return []

    chunks = coarse_split(text)
    all_clauses: list[dict] = []

    for chunk in chunks:
        prompt = build_extraction_prompt(chunk["content"], clause_types)

        try:
            import asyncio
            raw = asyncio.get_event_loop().run_until_complete(_call_llm_chat(prompt))
        except RuntimeError:
            # 已在 async context 中
            import asyncio
            async def _run():
                return await _call_llm_chat(prompt)
            raw = asyncio.get_event_loop().run_until_complete(_run())

        parsed = _parse_llm_response(raw)
        for clause in parsed:
            clause.setdefault("location", {"chapter": chunk["title"]})
        all_clauses.extend(parsed)

    return all_clauses
```

- [ ] **Step 3: 添加纯函数测试（不依赖LLM）**

在已有 test 文件中追加 `test_build_extraction_prompt` 和 `test_parse_llm_response`。

- [ ] **Step 4: 运行测试**

```bash
pytest tests/unit/test_compliance_clause_extractor.py -v
```

- [ ] **Step 5: Commit**

```bash
git add backend/app/services/analysis/compliance_clause_extractor.py \
        backend/tests/unit/test_compliance_clause_extractor.py
git commit -m "feat(compliance): add LLM fine clause extraction"
```

---

### Task 4: 确定性规则引擎 (R01-R08)

**Files:**
- Create: `backend/app/services/analysis/compliance_rule_engine.py`
- Create/Modify: `backend/tests/unit/test_compliance_rule_engine.py`

**Interfaces:**
- Produces: `evaluate_clause_deterministic(clause: dict, rule: dict) -> dict | None` — 单条确定性规则判定
- Produces: `evaluate_clause_llm(clause: dict, rule: dict, clauses_summary: str) -> dict | None` — 单条LLM规则判定
- Produces: `run_rule_engine(clauses: list[dict], rules: list[dict]) -> list[dict]` — 完整规则引擎

- [ ] **Step 1: 先写测试 — 每条确定性规则一个测试用例**

```python
"""测试合规规则引擎"""
import pytest
import pytest_asyncio
from app.services.analysis.compliance_rule_engine import (
    evaluate_clause_deterministic,
    run_rule_engine,
)

R01_RULE = {
    "id": "R01", "category": "deterministic", "default_risk": "red",
    "conditions": {"clause_type": "timeline", "field": "天数", "field_label": "发售期"},
    "params": {"min_days": 5},
}

R04_RULE = {
    "id": "R04", "category": "deterministic", "default_risk": "red",
    "conditions": {"clause_type": "technical"},
    "params": {"min_brands": 3, "equivalent_keywords": ["或同等", "或相当于"]},
}

R08_RULE = {
    "id": "R08", "category": "deterministic", "default_risk": "red",
    "conditions": {"clause_type": ["qualification", "performance", "scoring"]},
    "params": {"region_keywords": ["本地", "本省", "本市"]},
}


class TestDeterministicRules:
    def test_r01_sale_period_too_short(self):
        clause = {
            "type": "timeline", "original_text": "发售期3个工作日",
            "params": {"天数": 3, "时间类型": "发售期"},
        }
        result = evaluate_clause_deterministic(clause, R01_RULE)
        assert result is not None
        assert result["risk"] == "red"

    def test_r01_sale_period_ok(self):
        clause = {
            "type": "timeline", "original_text": "发售期7个工作日",
            "params": {"天数": 7, "时间类型": "发售期"},
        }
        result = evaluate_clause_deterministic(clause, R01_RULE)
        assert result is None  # 不命中 = 合规

    def test_r01_wrong_clause_type(self):
        clause = {"type": "qualification", "params": {"天数": 3}}
        result = evaluate_clause_deterministic(clause, R01_RULE)
        assert result is None

    def test_r04_brand_specified_without_equivalent(self):
        clause = {
            "type": "technical",
            "original_text": "服务器须采用Intel Xeon处理器",
            "params": {"品牌": "Intel"},
        }
        result = evaluate_clause_deterministic(clause, R04_RULE)
        assert result is not None
        assert result["risk"] == "red"

    def test_r04_brand_with_equivalent_ok(self):
        clause = {
            "type": "technical",
            "original_text": "服务器须采用Intel Xeon处理器或同等",
            "params": {"品牌": "Intel"},
        }
        result = evaluate_clause_deterministic(clause, R04_RULE)
        assert result is None

    def test_r08_region_keyword(self):
        clause = {
            "type": "qualification",
            "original_text": "投标人须在本省具有3年以上服务经验",
        }
        result = evaluate_clause_deterministic(clause, R08_RULE)
        assert result is not None
        assert result["risk"] == "red"

    def test_r08_no_region_keyword(self):
        clause = {
            "type": "qualification",
            "original_text": "投标人须具有3年以上服务经验",
        }
        result = evaluate_clause_deterministic(clause, R08_RULE)
        assert result is None


class TestRuleEngine:
    def test_run_engine_deterministic_only(self):
        clauses = [
            {"type": "timeline", "original_text": "发售期3日", "params": {"天数": 3, "时间类型": "发售期"}},
            {"type": "qualification", "original_text": "须有营业执照", "params": {}},
        ]
        rules = [R01_RULE, R08_RULE]
        results = run_rule_engine(clauses, rules)
        # clause 0 hits R01 → red
        assert results[0]["risk_level"] == "red"
        assert len(results[0]["matched_rules"]) >= 1
        # clause 1 is clean
        assert results[1]["risk_level"] == "green"
```

- [ ] **Step 2: 实现确定性规则引擎**

```python
"""合规规则引擎 — 确定性 + LLM 语义规则"""
from __future__ import annotations

from typing import Any


def _clause_type_matches(clause: dict, rule_condition: dict) -> bool:
    """检查条款类型是否匹配规则的适用条件。"""
    allowed = rule_condition.get("clause_type", [])
    if isinstance(allowed, str):
        allowed = [allowed]
    clause_type = clause.get("type", "")
    return clause_type in allowed


def _evaluate_r01(clause: dict, rule: dict) -> dict | None:
    """R01: 发售期不足"""
    params = clause.get("params", {})
    days = params.get("天数") or params.get("days")
    time_type = params.get("时间类型", "")
    if time_type != rule["conditions"].get("field_label", "发售期"):
        return None
    if days is not None and isinstance(days, (int, float)) and days < rule["params"]["min_days"]:
        return {"risk": rule["default_risk"], "reason": f"发售期{days}日，少于法定最低{rule['params']['min_days']}日"}
    return None


def _evaluate_r04(clause: dict, rule: dict) -> dict | None:
    """R04: 品牌指向性"""
    original = clause.get("original_text", "")
    params = clause.get("params", {})
    brands = params.get("品牌") or params.get("brand")
    if not brands:
        return None
    equivalents = rule["params"].get("equivalent_keywords", [])
    has_equivalent = any(kw in original for kw in equivalents)
    if has_equivalent:
        return None
    return {"risk": rule["default_risk"], "reason": f"指定品牌'{brands}'且无'或同等'字样，构成指向性"}


def _evaluate_r08(clause: dict, rule: dict) -> dict | None:
    """R08: 地域限制"""
    original = clause.get("original_text", "")
    keywords = rule["params"].get("region_keywords", [])
    for kw in keywords:
        if kw in original:
            return {"risk": rule["default_risk"], "reason": f"条款含地域限制词'{kw}'，涉嫌歧视外地投标人"}
    return None


# 确定性规则分发表
_DETERMINISTIC_EVALUATORS = {
    "R01": _evaluate_r01,
    "R04": _evaluate_r04,
    "R08": _evaluate_r08,
}


def evaluate_clause_deterministic(clause: dict, rule: dict) -> dict | None:
    """对单条条款执行确定性规则判定。

    Returns:
        dict | None: {"risk": "red|yellow|green", "reason": "..."} 或 None(不命中)
    """
    if not _clause_type_matches(clause, rule["conditions"]):
        return None

    evaluator = _DETERMINISTIC_EVALUATORS.get(rule["id"])
    if evaluator:
        return evaluator(clause, rule)

    # 通用确定性判定：检查 clause params 中的字段与 rule params 阈值的比较
    rule_params = rule.get("params", {})
    clause_params = clause.get("params", {})
    field = rule["conditions"].get("field")

    if field and field in rule_params and field in clause_params:
        clause_val = clause_params[field]
        threshold = rule_params[field]
        if isinstance(clause_val, (int, float)) and isinstance(threshold, (int, float)):
            if clause_val < threshold:
                return {"risk": rule["default_risk"], "reason": f"{field}={clause_val}, 低于阈值{threshold}"}

    return None


def _is_clause_green(clause: dict) -> bool:
    """条款是否无需送LLM（已被确定性规则判定为red/yellow 或 无匹配参数）。"""
    return clause.get("risk_level") in ("red", "yellow")


def evaluate_clause_llm(clause: dict, rule: dict) -> dict | None:
    """LLM语义规则判定（占位，Task 5实现）。"""
    return None  # Task 5 实现


def run_rule_engine(
    clauses: list[dict],
    rules: list[dict],
) -> list[dict]:
    """对条款列表执行完整的规则引擎判定。

    先跑确定性规则（即时），再对未命中条款跑 LLM 语义规则（异步）。

    Args:
        clauses: 条款卡片列表 (from extract_clauses)
        rules: 启用的规则列表

    Returns:
        list[dict]: 每个条款附带 risk_level + matched_rules
    """
    deterministic_rules = [r for r in rules if r.get("category") == "deterministic" and r.get("is_active", True)]
    llm_rules = [r for r in rules if r.get("category") == "llm_semantic" and r.get("is_active", True)]

    for clause in clauses:
        clause.setdefault("risk_level", "green")
        clause.setdefault("matched_rules", [])

    # Phase 1: 确定性规则
    for clause in clauses:
        for rule in deterministic_rules:
            result = evaluate_clause_deterministic(clause, rule)
            if result:
                result["rule_id"] = rule["id"]
                result["rule_name"] = rule.get("name", "")
                result["legal_basis"] = rule.get("legal_basis", [])
                clause["matched_rules"].append(result)
                # 取最高风险
                if result["risk"] == "red":
                    clause["risk_level"] = "red"
                elif result["risk"] == "yellow" and clause["risk_level"] != "red":
                    clause["risk_level"] = "yellow"

    # Phase 2: LLM 语义规则（仅对仍为 green 的条款）
    for clause in clauses:
        if _is_clause_green(clause):
            continue  # skip already flagged clauses
        for rule in llm_rules:
            result = evaluate_clause_llm(clause, rule)
            if result:
                result["rule_id"] = rule["id"]
                result["rule_name"] = rule.get("name", "")
                result["legal_basis"] = rule.get("legal_basis", [])
                clause["matched_rules"].append(result)
                if result["risk"] == "red":
                    clause["risk_level"] = "red"
                elif result["risk"] == "yellow" and clause["risk_level"] != "red":
                    clause["risk_level"] = "yellow"

    return clauses
```

- [ ] **Step 3: 运行测试**

```bash
pytest tests/unit/test_compliance_rule_engine.py -v
```

- [ ] **Step 4: Commit**

```bash
git add backend/app/services/analysis/compliance_rule_engine.py \
        backend/tests/unit/test_compliance_rule_engine.py
git commit -m "feat(compliance): add deterministic rule engine (R01,R04,R08)"
```

---

### Task 5: LLM 语义规则 + 评分引擎

**Files:**
- Modify: `backend/app/services/analysis/compliance_rule_engine.py` (实现 evaluate_clause_llm)
- Create: `backend/app/services/analysis/compliance_scorer.py`
- Create/Modify: `backend/tests/unit/test_compliance_scorer.py`

**Interfaces:**
- Modifies: `evaluate_clause_llm(clause, rule) -> dict | None` — 调用 Gitee AI 做语义判定
- Produces: `calculate_compliance_score(clauses: list[dict], config: dict) -> dict` — 扣分模型

- [ ] **Step 1: 实现 LLM 语义规则**

在 `compliance_rule_engine.py` 中替换占位的 `evaluate_clause_llm`：

```python
async def _call_llm_for_rule(prompt: str) -> dict | None:
    """调用 Gitee AI 做语义判定，返回结构化结果。"""
    import httpx, json
    from app.core.config import settings

    url = f"{settings.GITEE_AI_BASE_URL}/chat/completions"
    headers = {"Authorization": f"Bearer {settings.GITEE_AI_API_KEY}"}
    payload = {
        "model": "Qwen3-32B",
        "messages": [{"role": "user", "content": prompt}],
        "temperature": 0.1,
        "max_tokens": 500,
    }
    try:
        async with httpx.AsyncClient(timeout=30) as client:
            resp = await client.post(url, json=payload, headers=headers)
            resp.raise_for_status()
            data = resp.json()
            content = data["choices"][0]["message"]["content"].strip()
            if content.startswith("```"):
                lines = content.split("\n")
                content = "\n".join(lines[1:-1])
            return json.loads(content)
    except Exception:
        return None


def evaluate_clause_llm(clause: dict, rule: dict) -> dict | None:
    """LLM语义规则判定。

    使用规则的 llm_prompt 模板，填充条款信息后送 LLM 判定。
    """
    prompt_template = rule.get("llm_prompt", "")
    if not prompt_template:
        return None

    prompt = prompt_template.format(
        clause_text=clause.get("original_text", ""),
        clause_type=clause.get("type", ""),
        params=clause.get("params", {}),
    )

    result = None
    try:
        import asyncio
        loop = asyncio.get_event_loop()
        if loop.is_running():
            import concurrent.futures
            future = asyncio.run_coroutine_threadsafe(_call_llm_for_rule(prompt), loop)
            result = future.result(timeout=30)
        else:
            result = loop.run_until_complete(_call_llm_for_rule(prompt))
    except Exception:
        return None

    if result and isinstance(result, dict):
        risk = result.get("risk", "green")
        if risk in ("red", "yellow"):
            return {
                "risk": risk,
                "reason": result.get("reason", ""),
                "confidence": result.get("confidence", 0.5),
            }
    return None
```

- [ ] **Step 2: 实现扣分模型 `compliance_scorer.py`**

```python
"""合规评分引擎 — 基于条款风险等级的扣分模型"""
from typing import Any

from app.core.compliance_config import load_compliance_config


def calculate_compliance_score(
    clauses: list[dict],
    config: dict | None = None,
) -> dict[str, Any]:
    """根据条款风险等级计算合规评分。

    Args:
        clauses: 已判定的条款列表 (risk_level + matched_rules)
        config: 评分配置，默认从 compliance_config.json 读取

    Returns:
        dict: {score, risk_level, red_count, yellow_count, green_count, total_count}
    """
    if config is None:
        config = load_compliance_config()
    scoring = config.get("scoring", {})

    initial = scoring.get("initial_score", 100)
    red_deduction = scoring.get("red_deduction", 20)
    yellow_deduction = scoring.get("yellow_deduction", 8)

    red_count = sum(1 for c in clauses if c.get("risk_level") == "red")
    yellow_count = sum(1 for c in clauses if c.get("risk_level") == "yellow")
    green_count = sum(1 for c in clauses if c.get("risk_level") == "green")

    score = max(0, initial - red_count * red_deduction - yellow_count * yellow_deduction)

    risk_low = scoring.get("risk_low", 85)
    risk_medium = scoring.get("risk_medium", 60)
    risk_high = scoring.get("risk_high", 40)

    if score >= risk_low:
        risk_level = "low"
    elif score >= risk_medium:
        risk_level = "moderate"
    elif score >= risk_high:
        risk_level = "high"
    else:
        risk_level = "critical"

    return {
        "score": score,
        "risk_level": risk_level,
        "red_count": red_count,
        "yellow_count": yellow_count,
        "green_count": green_count,
        "total_count": len(clauses),
    }
```

- [ ] **Step 3: 写评分测试 + 运行**

```python
# test_compliance_scorer.py
class TestComplianceScorer:
    def test_all_green_full_score(self):
        from app.services.analysis.compliance_scorer import calculate_compliance_score
        clauses = [
            {"risk_level": "green"},
            {"risk_level": "green"},
            {"risk_level": "green"},
        ]
        result = calculate_compliance_score(clauses)
        assert result["score"] == 100
        assert result["risk_level"] == "low"

    def test_one_red_deducts(self):
        from app.services.analysis.compliance_scorer import calculate_compliance_score
        clauses = [
            {"risk_level": "red", "matched_rules": [{"rule_id": "R01"}]},
            {"risk_level": "green"},
        ]
        result = calculate_compliance_score(clauses)
        assert result["score"] == 80  # 100 - 20
        assert result["red_count"] == 1

    def test_mixed_deductions(self):
        from app.services.analysis.compliance_scorer import calculate_compliance_score
        clauses = [
            {"risk_level": "red"},
            {"risk_level": "red"},
            {"risk_level": "yellow"},
            {"risk_level": "yellow"},
            {"risk_level": "green"},
        ]
        result = calculate_compliance_score(clauses)
        assert result["score"] == 44  # 100 - 2*20 - 2*8
        assert result["risk_level"] == "high"

    def test_zero_floor(self):
        from app.services.analysis.compliance_scorer import calculate_compliance_score
        clauses = [{"risk_level": "red"} for _ in range(10)]
        result = calculate_compliance_score(clauses)
        assert result["score"] == 0
```

```bash
pytest tests/unit/test_compliance_scorer.py -v
```

- [ ] **Step 4: Commit**

```bash
git add backend/app/services/analysis/compliance_rule_engine.py \
        backend/app/services/analysis/compliance_scorer.py \
        backend/tests/unit/test_compliance_scorer.py
git commit -m "feat(compliance): add LLM semantic rules + deduction scoring model"
```

---

### Task 6: 合规分析编排器 + API

**Files:**
- Create: `backend/app/services/analysis/compliance_orchestrator.py`
- Create: `backend/app/api/v1/compliance.py`
- Modify: `backend/app/schemas/compliance.py` (Pydantic schemas)

- [ ] **Step 1: 创建 Pydantic Schemas**

```python
"""合规审查 Schemas"""
import uuid
from datetime import datetime
from decimal import Decimal
from typing import Optional

from pydantic import BaseModel, Field


class ComplianceClauseResponse(BaseModel):
    id: uuid.UUID
    analysis_id: uuid.UUID
    clause_type: str
    original_text: str
    location: Optional[dict] = None
    params: Optional[dict] = None
    risk_level: Optional[str] = None
    matched_rules: Optional[dict] = None
    model_config = {"from_attributes": True}


class ComplianceAnalysisResponse(BaseModel):
    id: uuid.UUID
    project_id: uuid.UUID
    document_id: uuid.UUID
    status: str
    progress: int = 0
    compliance_score: Optional[Decimal] = None
    risk_level: Optional[str] = None
    clause_count: int = 0
    violation_count: int = 0
    error_message: Optional[str] = None
    started_at: Optional[datetime] = None
    completed_at: Optional[datetime] = None
    created_at: datetime
    clauses: list[ComplianceClauseResponse] = Field(default_factory=list)
    model_config = {"from_attributes": True}


class ComplianceAnalysisCreate(BaseModel):
    project_id: str
    document_id: str
```

- [ ] **Step 2: 创建编排器**

```python
"""合规审查编排器 — 完整分析管线"""
from __future__ import annotations

import uuid
from datetime import datetime, timezone
from decimal import Decimal
from typing import Any

from loguru import logger
from sqlalchemy import select

from app.core.compliance_config import get_active_clause_types, get_active_rules
from app.models.compliance import ComplianceAnalysis, ComplianceClause
from app.models.project import BidDocument
from app.services.analysis.compliance_clause_extractor import extract_clauses
from app.services.analysis.compliance_rule_engine import run_rule_engine
from app.services.analysis.compliance_scorer import calculate_compliance_score


class ComplianceOrchestrator:
    def __init__(self, db_session_factory):
        self.db_session_factory = db_session_factory

    async def run_analysis(
        self, analysis_id: str
    ) -> dict[str, Any]:
        async with self.db_session_factory() as db:
            stmt = select(ComplianceAnalysis).where(ComplianceAnalysis.id == analysis_id)
            result = await db.execute(stmt)
            analysis = result.scalar_one_or_none()
            if not analysis:
                return {"status": "error", "message": "分析任务不存在"}

            # Update status
            analysis.status = "analyzing"
            analysis.progress = 0
            analysis.started_at = datetime.now(timezone.utc)
            await db.commit()

            # Get document text
            doc_stmt = select(BidDocument).where(BidDocument.id == analysis.document_id)
            doc_result = await db.execute(doc_stmt)
            doc = doc_result.scalar_one_or_none()
            if not doc or not doc.content_text:
                analysis.status = "failed"
                analysis.error_message = "文档未解析或内容为空"
                await db.commit()
                return {"status": "failed"}

        # Step 1: Extract clauses
        clause_types = get_active_clause_types()
        clauses_raw = extract_clauses(doc.content_text, clause_types)

        # Step 2: Run rule engine
        rules = get_active_rules()
        clauses_evaluated = run_rule_engine(clauses_raw, rules)

        # Step 3: Calculate score
        score_result = calculate_compliance_score(clauses_evaluated)

        # Step 4: Persist results
        async with self.db_session_factory() as db:
            for clause_data in clauses_evaluated:
                clause = ComplianceClause(
                    id=str(uuid.uuid4()),
                    analysis_id=analysis_id,
                    clause_type=clause_data.get("type", "other"),
                    original_text=clause_data.get("original_text", ""),
                    location=clause_data.get("location"),
                    params=clause_data.get("params"),
                    risk_level=clause_data.get("risk_level", "green"),
                    matched_rules=clause_data.get("matched_rules", []),
                )
                db.add(clause)

            analysis.status = "completed"
            analysis.progress = 100
            analysis.compliance_score = Decimal(str(score_result["score"]))
            analysis.risk_level = score_result["risk_level"]
            analysis.clause_count = score_result["total_count"]
            analysis.violation_count = score_result["red_count"] + score_result["yellow_count"]
            analysis.completed_at = datetime.now(timezone.utc)
            await db.commit()

        return {"status": "completed", **score_result}
```

- [ ] **Step 3: 创建 API 端点**

```python
"""合规审查 API"""
from fastapi import APIRouter, BackgroundTasks, Depends, HTTPException
from sqlalchemy.ext.asyncio import AsyncSession
from sqlalchemy import select

from app.db.session import get_db, async_session_factory
from app.models.compliance import ComplianceAnalysis, ComplianceClause
from app.schemas.compliance import (
    ComplianceAnalysisCreate, ComplianceAnalysisResponse, ComplianceClauseResponse,
)
from app.schemas.common import ApiResponse
from app.services.analysis.compliance_orchestrator import ComplianceOrchestrator

router = APIRouter(prefix="/compliance")


async def _run_compliance_background(analysis_id: str):
    orchestrator = ComplianceOrchestrator(db_session_factory=async_session_factory)
    await orchestrator.run_analysis(analysis_id)


@router.post("/analyze", response_model=ApiResponse[ComplianceAnalysisResponse])
async def start_compliance_analysis(
    data: ComplianceAnalysisCreate,
    background_tasks: BackgroundTasks,
    db: AsyncSession = Depends(get_db),
):
    analysis = ComplianceAnalysis(
        project_id=data.project_id,
        document_id=data.document_id,
        status="pending",
    )
    db.add(analysis)
    await db.commit()
    await db.refresh(analysis)

    background_tasks.add_task(_run_compliance_background, str(analysis.id))

    return ApiResponse.success(
        data=ComplianceAnalysisResponse.model_validate(analysis),
        message="合规审查已启动",
    )


@router.get("/{analysis_id}", response_model=ApiResponse[ComplianceAnalysisResponse])
async def get_compliance_result(
    analysis_id: str,
    db: AsyncSession = Depends(get_db),
):
    stmt = select(ComplianceAnalysis).where(ComplianceAnalysis.id == analysis_id)
    result = await db.execute(stmt)
    analysis = result.scalar_one_or_none()
    if not analysis:
        raise HTTPException(status_code=404, detail="分析任务不存在")
    return ApiResponse.success(data=ComplianceAnalysisResponse.model_validate(analysis))
```

- [ ] **Step 4: 注册路由**

在 `backend/app/main.py` 中添加：
```python
from app.api.v1.compliance import router as compliance_router
app.include_router(compliance_router, prefix="/api/v1")
```

- [ ] **Step 5: Commit**

```bash
git add backend/app/services/analysis/compliance_orchestrator.py \
        backend/app/api/v1/compliance.py \
        backend/app/schemas/compliance.py \
        backend/app/main.py
git commit -m "feat(compliance): add orchestrator + API endpoints"
```

---

### Task 7: 前端 — 类型 + API + 合规审查 Tab

**Files:**
- Modify: `frontend/src/types/index.ts`
- Modify: `frontend/src/services/api.ts`
- Modify: `frontend/src/pages/ProjectDetail/index.tsx`
- Create: `frontend/src/pages/ProjectDetail/components/CompliancePanel.tsx`

- [ ] **Step 1: 添加前端类型**

```typescript
// 追加到 types/index.ts
export interface ComplianceClause {
  id: string;
  analysis_id: string;
  clause_type: string;
  original_text: string;
  location: any;
  params: any;
  risk_level: string | null;
  matched_rules: any;
}

export interface ComplianceAnalysis {
  id: string;
  project_id: string;
  document_id: string;
  status: string;
  progress: number;
  compliance_score: number | null;
  risk_level: string | null;
  clause_count: number;
  violation_count: number;
  error_message: string | null;
  started_at: string | null;
  completed_at: string | null;
  created_at: string;
  clauses: ComplianceClause[];
}
```

- [ ] **Step 2: 添加 API**

```typescript
// 追加到 services/api.ts analysisApi export
export const complianceApi = {
  start: (projectId: string, documentId: string) =>
    request<ComplianceAnalysis>('/compliance/analyze', {
      method: 'POST',
      body: JSON.stringify({ project_id: projectId, document_id: documentId }),
    }),
  getResult: (analysisId: string) =>
    request<ComplianceAnalysis>(`/compliance/${analysisId}`),
};
```

- [ ] **Step 3: 创建 CompliancePanel 组件**

```tsx
// frontend/src/pages/ProjectDetail/components/CompliancePanel.tsx
import React from 'react';
import { Card, Table, Tag, Progress, Descriptions, Empty, Typography } from 'antd';
import { SafetyOutlined } from '@ant-design/icons';
import type { ComplianceAnalysis } from '@/types';
import { RISK_LEVEL_COLORS, RISK_LEVEL_LABELS } from '@/types';

const { Text } = Typography;

interface Props {
  analysis: ComplianceAnalysis | null;
  loading: boolean;
  onStart: () => void;
}

const CompliancePanel: React.FC<Props> = ({ analysis, loading, onStart }) => {
  if (!analysis) {
    return (
      <Card>
        <Empty description="暂无合规审查数据">
          <Button type="primary" onClick={onStart} icon={<SafetyOutlined />}>
            开始合规审查
          </Button>
        </Empty>
      </Card>
    );
  }

  const riskColor = analysis.risk_level ? RISK_LEVEL_COLORS[analysis.risk_level.toUpperCase()] : '#999';
  const riskLabel = analysis.risk_level ? RISK_LEVEL_LABELS[analysis.risk_level.toUpperCase()] : '-';

  return (
    <div>
      <Card style={{ marginBottom: 16 }}>
        <Descriptions column={4} size="small">
          <Descriptions.Item label="合规评分">
            <Progress type="circle" percent={analysis.compliance_score ?? 0} size={60}
              strokeColor={riskColor} />
          </Descriptions.Item>
          <Descriptions.Item label="风险等级">
            <Tag color={riskColor}>{riskLabel}</Tag>
          </Descriptions.Item>
          <Descriptions.Item label="条款总数">{analysis.clause_count}</Descriptions.Item>
          <Descriptions.Item label="违规条款">{analysis.violation_count}</Descriptions.Item>
        </Descriptions>
      </Card>

      <Card title="条款审查清单" bodyStyle={{ padding: 0 }}>
        <Table
          dataSource={analysis.clauses || []}
          rowKey="id"
          loading={loading}
          pagination={false}
          size="small"
          columns={[
            {
              title: '风险', dataIndex: 'risk_level', key: 'risk', width: 60,
              render: (v: string) => {
                const colors: Record<string, string> = { red: '#f5222d', yellow: '#faad14', green: '#52c41a' };
                const labels: Record<string, string> = { red: '🔴', yellow: '🟡', green: '🟢' };
                return <Tag color={colors[v]}>{labels[v] || v}</Tag>;
              },
            },
            {
              title: '条款', dataIndex: 'original_text', key: 'text', ellipsis: true,
              render: (v: string) => (
                <Text style={{ color: v ? undefined : '#999' }}>{v || '(无内容)'}</Text>
              ),
            },
            {
              title: '类型', dataIndex: 'clause_type', key: 'type', width: 100,
              render: (v: string) => <Tag>{v}</Tag>,
            },
            {
              title: '命中规则', key: 'rules', width: 150,
              render: (_: any, record: any) => {
                const rules = record.matched_rules || [];
                if (rules.length === 0) return <Tag color="green">合规</Tag>;
                return (
                  <span>
                    {rules.map((r: any) => (
                      <Tag key={r.rule_id} color={r.risk === 'red' ? 'red' : 'orange'}>
                        {r.rule_name}
                      </Tag>
                    ))}
                  </span>
                );
              },
            },
          ]}
        />
      </Card>
    </div>
  );
};

export default CompliancePanel;
```

- [ ] **Step 4: 在 ProjectDetail 中新增合规审查 Tab**

在 `tabItems` 数组中追加：
```tsx
{
  key: 'compliance',
  label: <span><SafetyOutlined /> 合规审查</span>,
  children: (
    <CompliancePanel
      analysis={complianceAnalysis}
      loading={loadingCompliance}
      onStart={startComplianceAnalysis}
    />
  ),
},
```

同时在组件中添加 state + handler：
```tsx
const [complianceAnalysis, setComplianceAnalysis] = useState<ComplianceAnalysis | null>(null);
const [loadingCompliance, setLoadingCompliance] = useState(false);

const startComplianceAnalysis = async () => {
  if (!projectId) return;
  setLoadingCompliance(true);
  try {
    // 取第一个已解析的文档作为招标文件
    const docs = documents.filter(d => d.parse_status === 'completed');
    if (docs.length === 0) {
      message.warning('请先上传并解析招标文件');
      return;
    }
    const result = await complianceApi.start(projectId, docs[0].id);
    pollCompliance(result.id);
  } catch { message.error('启动合规审查失败'); }
  setLoadingCompliance(false);
};

const pollCompliance = (analysisId: string) => {
  const timer = setInterval(async () => {
    const result = await complianceApi.getResult(analysisId);
    setComplianceAnalysis(result);
    if (result.status === 'completed' || result.status === 'failed') {
      clearInterval(timer);
      setLoadingCompliance(false);
    }
  }, 2000);
};
```

- [ ] **Step 5: Commit**

```bash
git add frontend/src/types/index.ts frontend/src/services/api.ts \
        frontend/src/pages/ProjectDetail/index.tsx \
        frontend/src/pages/ProjectDetail/components/CompliancePanel.tsx
git commit -m "feat(compliance): add frontend compliance review tab + panel"
```

---

## Verification

完成所有 Task 后执行：

```bash
# 1. 单元测试
cd backend && pytest tests/unit/ -q
# Expected: 107 + compliance tests all passing

# 2. 启动后端
cd backend && uvicorn app.main:app --port 8006
curl http://localhost:8006/api/v1/compliance/health

# 3. 前端构建
cd frontend && npx vite build
# Expected: build success

# 4. E2E 流程
# 创建项目 → 上传招标文件 → 解析文档 → 调用 /compliance/analyze
# → 轮询结果 → 验证条款列表 + 红黄灯 + 合规评分
```
